	

	<% if @users.nil?
	 @users = Auth.paginate(:page => params[:page] , :per_page => 1) %>
	<% end %>

	<%= will_paginate(@users) %>

	<br/><br/>

	<% @users.each do |user| %>
		<li > <%= link_to user.name , auth_path(user)  %> 


 </li>
	<% end %>

<br/><br/>



	<%= will_paginate(@users) %>

