<h2> Transcations </h2>

<% unless @transcation.errors.empty? %>
	<div id="error"> <%= @transcation.errors.full_messages %> </div> <br/>
<% end %>


Book: <%= @transcation.book.title %> <br/>
Book id: <%= @transcation.book.id %> <br/>
User Name: <%= @transcation.auth.name %> <br/>
User Regno: <%= @transcation.auth.regno %><br/>


Returned: <%= @transcation.returned %> <br/>

<% if @transcation.returned %>
	Returned Date: <%= @transcation.returneddoj %> <br/>
<%end %>



	Issued on: <%= @transcation.startdte.strftime("%d-%m-%Y ")%> <br/>
	Due Date: <%= @transcation.enddte.strftime("%d-%m-%Y ")%> <br/>

<% if current_auth.admin == true %>
		
<%= link_to "delete" , transcation_path(@transcation), { :method => :delete , :remote => true} %>


	<% unless @transcation.returned %>


		<%= link_to "Return" , returnbook_path(@transcation) , {:method => :post ,:remote => true }  %>

		<%= link_to "Re Issue" , transcation_path(@transcation) , {:method => :put ,:remote => true }  %>

	<%end %>
		
<% end %>

<br/><br/>

<%= link_to 'Back to transcation' ,transcations_path %>