<h2> Tags </h2>

<%= link_to 'Add Tag' , new_tag_path %> <br/>

<% @tags.each do |tag| %>
	<li> <%= tag.name  %>
		<%= link_to "edit" , tag_path(tag) %>
		<%= link_to "delete" , tag_path(tag), :method => :delete %>
	</li>
<% end %>
